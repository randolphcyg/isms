# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Dashboard API
    description: 大屏统计数据服务
    version: 0.0.1
paths:
    /v1/dashboard/overview:
        get:
            tags:
                - Dashboard
            description: 获取概览统计数据
            operationId: Dashboard_GetOverviewStats
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetOverviewStatsResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/software-by-country:
        get:
            tags:
                - Dashboard
            description: 获取软件按国家分布统计
            operationId: Dashboard_GetSoftwareByCountryStats
            parameters:
                - name: topN
                  in: query
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetSoftwareByCountryStatsResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/software-by-developer:
        get:
            tags:
                - Dashboard
            description: 获取软件按开发商分布统计
            operationId: Dashboard_GetSoftwareByDeveloperStats
            parameters:
                - name: topN
                  in: query
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetSoftwareByDeveloperStatsResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/software-by-industry:
        get:
            tags:
                - Dashboard
            description: 获取软件按行业分布统计
            operationId: Dashboard_GetSoftwareByIndustryStats
            parameters:
                - name: topN
                  in: query
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetSoftwareByIndustryStatsResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/software-by-status:
        get:
            tags:
                - Dashboard
            description: 获取软件按状态分布统计
            operationId: Dashboard_GetSoftwareByStatusStats
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetSoftwareByStatusStatsResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/dashboard/software-trend:
        get:
            tags:
                - Dashboard
            description: 获取软件按年份发布趋势统计
            operationId: Dashboard_GetSoftwareTrendStats
            parameters:
                - name: recentYears
                  in: query
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetSoftwareTrendStatsResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CountryStatItem:
            type: object
            properties:
                countryId:
                    type: integer
                    format: int32
                countryNameZh:
                    type: string
                countryNameEn:
                    type: string
                softwareCount:
                    type: string
                percentage:
                    type: number
                    format: double
            description: 国家统计项
        DeveloperStatItem:
            type: object
            properties:
                developerId:
                    type: integer
                    format: int32
                developerNameZh:
                    type: string
                developerNameEn:
                    type: string
                softwareCount:
                    type: string
                percentage:
                    type: number
                    format: double
            description: 开发商统计项
        GetOverviewStatsResp:
            type: object
            properties:
                totalSoftware:
                    type: string
                totalDevelopers:
                    type: string
                totalIndustries:
                    type: string
                totalCountries:
                    type: string
                newSoftwareCount:
                    type: string
                lastUpdated:
                    type: string
            description: 概览统计数据响应
        GetSoftwareByCountryStatsResp:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/CountryStatItem'
            description: 软件按国家分布统计响应
        GetSoftwareByDeveloperStatsResp:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/DeveloperStatItem'
            description: 软件按开发商分布统计响应
        GetSoftwareByIndustryStatsResp:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/IndustryStatItem'
            description: 软件按行业分布统计响应
        GetSoftwareByStatusStatsResp:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/StatusStatItem'
            description: 软件按状态分布统计响应
        GetSoftwareTrendStatsResp:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/TrendStatItem'
            description: 软件按年份发布趋势统计响应
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        IndustryStatItem:
            type: object
            properties:
                industryId:
                    type: integer
                    format: int32
                industryName:
                    type: string
                softwareCount:
                    type: string
                percentage:
                    type: number
                    format: double
            description: 行业统计项
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        StatusStatItem:
            type: object
            properties:
                status:
                    type: string
                statusLabel:
                    type: string
                softwareCount:
                    type: string
                percentage:
                    type: number
                    format: double
            description: 状态统计项
        TrendStatItem:
            type: object
            properties:
                year:
                    type: integer
                    format: int32
                softwareCount:
                    type: string
            description: 年份趋势统计项
tags:
    - name: Dashboard
